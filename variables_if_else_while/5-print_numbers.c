#include <stdio.h>

 /**
   * main - Entry point
   *
   * Return: Always 0 (Success)
   */
int
main(void) /*this main does something */
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar(i);
	}
		putchar('\n');

	return (0);
}
