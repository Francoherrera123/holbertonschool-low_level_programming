#include "main.h"
#include <stdlib.h>

/**
 * add - Entry point
 * @val1: integer 1
 * @val2: integer 2
 * Return: Always 0 (Success)
 */
int add(int val1, int val2)
{
	int SUM;

	SUM = val1 + val2;
	return (SUM);
}
